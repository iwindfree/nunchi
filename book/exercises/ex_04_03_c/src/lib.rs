// nunchi 의 repo_roots 를 단순하게 만든 것입니다.
//
// 저장소 경로 목록에서 (이름, 경로) 표를 만드십시오.
// 이름은 경로의 마지막 조각입니다. 마지막 조각이 없으면 그 항목은 버립니다.
//
// 힌트: 짝(튜플)을 collect 하면 HashMap 이 됩니다(4.3장).
//       클로저 안에서 ? 를 쓸 수 있습니다.

use std::collections::HashMap;

pub fn repo_names(paths: &[String]) -> HashMap<String, String> {
    todo!()
}
