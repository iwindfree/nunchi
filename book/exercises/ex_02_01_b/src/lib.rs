// 아래 두 함수를 완성하십시오.
//
// 1. display_name
//    이름이 있으면 그대로 쓰고, 없으면 "(anonymous)" 를 씁니다.
//    대체 문자열은 값이 없을 때만 만들어야 합니다.
//
// 2. name_length
//    이름이 있으면 그 길이를, 없으면 0 을 돌려줍니다.
//    match 를 쓰지 말고 Option 의 메서드로 한 줄로 쓰십시오.
//
// 힌트: 2.1장의 "자주 쓰는 변환" 표를 보십시오.
//       .map() 과 .unwrap_or() 계열을 조합하면 됩니다.

pub fn display_name(name: Option<String>) -> String {
    todo!()
}

pub fn name_length(name: Option<String>) -> usize {
    todo!()
}
