/target
*.db
*.db-wal
*.db-shm
.nunchi/

# 머신별 절대 경로를 담으므로 커밋하지 않는다 (PLAN.md 3.10절).
# 랭킹 가중치·프레임워크 규칙·용어 사전은 nunchi.shared.toml 로 분리되어
# 커밋되며, 양쪽 머신이 같은 값을 쓴다.
nunchi.toml

# mdBook 산출물
/book/book
/book/exercises/target
book/book/
