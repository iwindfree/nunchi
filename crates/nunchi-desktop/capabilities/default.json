{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 권한. 폴더 선택 대화상자만 추가로 허용한다.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
